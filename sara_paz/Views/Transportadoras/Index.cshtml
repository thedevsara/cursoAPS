@model IEnumerable<sara_paz.Models.Transportadora>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.notavenda)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.notavenda.cod_nota)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idtrans">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idtrans">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idtrans">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
